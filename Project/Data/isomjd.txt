issue
Changing an attribute for one node changes the attribute for all nodes#TITLE_END#I've been working to develop a subclass of networkx.Graph using edge_attr_dict_factory and node_attr_dict_factory.  When I try to use the subclass, however, I get some unexpected behavior.  Changing an attribute for one node seems to always change the attribute for all nodes.  Could you help me determine what I am doing wrong?  Here is example code showing the problem: ```python from enum import Enum import networkx as nx  class FlowDir(Enum):     FORWARD=1     BACKWARD=-1  class GPNNodeType(Enum):     PLANT=1     CUSTOMER=2     JUNCTION=3  class GasPipelineNetwork(nx.Graph):      all_edge_dict={'Direction':FlowDir.FORWARD,'Length':100,'Diameter':12}     def GPN_edge_dict(self):         return self.all_edge_dict     edge_attr_dict_factory=GPN_edge_dict          all_node_dict={'NodeType':GPNNodeType.PLANT}     def GPN_node_dict(self):         return self.all_node_dict     node_attr_dict_factory=GPN_node_dict         G=GasPipelineNetwork() G.add_nodes_from([1,2,3,4]) ```  Now if I run ```python     G.nodes[2]['NodeType']=GPNNodeType.CUSTOMER       list(G.nodes(data=True)) ``` I get ```python [(1, {'NodeType': <GPNNodeType.CUSTOMER: 2>}),  (2, {'NodeType': <GPNNodeType.CUSTOMER: 2>}),  (3, {'NodeType': <GPNNodeType.CUSTOMER: 2>}),  (4, {'NodeType': <GPNNodeType.CUSTOMER: 2>})] ``` But if I run  ```python   G.nodes[1]['NodeType']=GPNNodeType.PLANT    list(G.nodes(data=True)) ``` I get ```python [(1, {'NodeType': <GPNNodeType.PLANT: 1>}),  (2, {'NodeType': <GPNNodeType.PLANT: 1>}),  (3, {'NodeType': <GPNNodeType.PLANT: 1>}),  (4, {'NodeType': <GPNNodeType.PLANT: 1>})] ```  Changing an attribute for one node changes the attribute for all nodes, which is not the desired behavior.

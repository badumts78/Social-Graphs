issue
bug of greedy_modularity_communities#TITLE_END#<!-- If you have a general question about NetworkX, please use the discussions tab to create a new discussion -->  <!--- Provide a general summary of the issue in the Title above -->  Clauset-Newman-Moore greedy modularity maximization algorithm should using max heap,and you use min heap  ### Current Behavior <!--- Tell us what happens instead of the expected behavior -->  use min heap in dq_dict ### Expected Behavior <!--- Tell us what should happen --> shoukd use maxheap ### Steps to Reproduce <!--- Provide a minimal example that reproduces the bug --> import networkx as nx from networkx.algorithms.community import greedy_modularity_communities from networkx.algorithms.community.quality import modularity  G = nx.Graph() G.add_edge('a', 'b', weight = 1) G.add_edge('b', 'c', weight = 1) G.add_edge('e', 'd', weight = 10) G.add_edge('c', 'a', weight = 1) G.add_edge('b', 'd', weight = 0) print(greedy_modularity_communities(G))  # return [frozenset({'e', 'b', 'd', 'c', 'a'})] ### Environment <!--- Please provide details about your local environment --> Python version:Python 3.9.7 NetworkX version:2.4   ### Additional context <!--- Add any other context about the problem here, screenshots, etc. --> i guess should  use max heap?

issue
Added clarifying comment about relabelling nodes in situations where …#TITLE_END#…the order of nodes is important
issue
Parsing bug in  `read_weighted_edgelist`#TITLE_END#Code to recreate the issue:  ```python def make_network():      G = nx.Graph()     rows = range(ROWS)     columns = range(COLUMNS)      #https://networkx.github.io/documentation/networkx-1.10/_modules/networkx/generators/classic.html#grid_graph     G.add_nodes_from( ((i,j) for i in rows for j in columns))     G.add_edges_from( ((i,j),(i-1,j)) for i in rows for j in columns if i>0 )     G.add_edges_from( ((i,j),(i,j-1)) for i in rows for j in columns if j>0 )             print(G.edges())      pos = {n:n for n in G.nodes()}      print("The network is size: {}x{}".format(max(rows)+1, max(columns)+1))     nx.draw(G, pos, **options)     plt.show()          # Now to make the graph easier to serialise (consecutive)     sG = nx.convert_node_labels_to_integers(G)     print(sG.edges())          #Now add resistances to edges     for edge in sG.edges():         sG[edge[0]][edge[1]]['weight'] = 1 # for 1 Ohm, so apparently has to be 'weight' to save to a file :(     nx.write_weighted_edgelist(sG, NAME) ```  Then to parse:  ```python try:         DG = nx.read_weighted_edgelist(NAME)  # parsing edge list with weights to create graph     except IOError:         raise FileNotFoundError("Could not find Graph with name {}".format(name_of_file))      G = nx.convert_node_labels_to_integers(DG) ```  If we print the edges using: print(G.edges()) in the parse function just above we get: `[(0, 1), (0, 2), **(1, 4),** (1, 3), (2, 3), (3, 5), (4, 5)]`  but if we print the file saved on disk we get:  ```bash $ cat notebooks/network.txt  0 2 0 1 1 3 **2 4** 2 3 3 5 4 5 ``` There is a bug. The edge (1,4) should actually be (2,4). This seems to occur in bigger network sizes too. The smallest networks size I can recreate this bug with is 3X2.  I'm not doing anything stupid am I? :(

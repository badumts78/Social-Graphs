issue
all_simple_paths missing some simple paths#TITLE_END#I ran the following code with the latest version of `networkx`. I find one of the functions `all_simple_paths` does not print out all simple paths. The following example illustrates my point.  ### Current Behavior When I run the following code ``` import networkx as nx  g = nx.MultiDiGraph() g.add_edge(0,1) g.add_edge(1,2) g.add_edge(3, 0) g.add_edge(3, 2) paths = nx.all_simple_paths(g, 0, 2) print(list(paths)) ``` It gives me `[[0, 1, 2]]`  ### Expected Behavior I think it should output `[[0, 1, 2], [0, 3, 2]]`  ### Steps to Reproduce ``` import networkx as nx  g = nx.MultiDiGraph() g.add_edge(0,1) g.add_edge(1,2) g.add_edge(3, 0) g.add_edge(3, 2) paths = nx.all_simple_paths(g, 0, 2) print(list(paths)) ```  ### Environment  Python version: 3.10.0 NetworkX version: networkx-3.1 

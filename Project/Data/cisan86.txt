issue
have trouble adding multiple edges between two nodes#TITLE_END#when i use the code below I can only get two nodes with one edge , but the output of `print(list(G.edges))` is `[('a', 'b', 0), ('a', 'b', 1), ('a', 'b', 2)]` ``` import networkx as nx import matplotlib.pyplot as plt  G = nx.MultiGraph() V = {'a', 'b'} G.add_nodes_from(V) G.add_edge('a', 'b') G.add_edge('a', 'b') G.add_edge('a', 'b') print(list(G.edges)) nx.draw(G, with_labels=True, node_size=100) plt.show() ``` the graph is like  ![image](https://user-images.githubusercontent.com/53175649/236759486-60dc380d-e773-458d-957e-35ee994770d3.png)  It is very kind of you to help me 

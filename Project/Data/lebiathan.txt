issue
Initialization vector (nstart) for all pagerank variants#TITLE_END#Hi,  I've been using networkx for a brief time now, but noticed that when it comes to the implemented [PageRank variants](https://networkx.github.io/documentation/stable/reference/algorithms/link_analysis.html) only the Python version accepts an initialization vector (**nstart**) as a parameter.  From what I've read - but haven't tried it out so far - the scipy and numpy implementations should be faster than the pure python version, but not being able to provide the initialization vector takes them off the table for a variety of cases.  I was wondering if that omission of **nstart** is by design and there's some underlying reason that I don't fully understand.  As I didn't find a related discussion / request in the past issues, I opened a new one. I'd be happy to submit a PR that adds the **nstart** parameters to the other methods, treating it in the same was as with the Python one.  George
issue
Add support for initializing pagerank_scipy#TITLE_END#This will add support for an initialization vector parameter 'nstart' to the ```pagerank_scipy``` method. Also includes relevant unit tests.

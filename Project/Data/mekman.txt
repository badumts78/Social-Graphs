comment
@hagberg I didn't open the original trac issue, but yes the code for the global efficiency you posted is correct. Also for the weighted case we could simply use `nx.single_source_dijkstra_path_length`. Would be very nice to include that into the package. 

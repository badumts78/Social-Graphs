issue
Bug on compute initial view of edges#TITLE_END#<!-- If you have a general question about NetworkX, please use the discussions tab to create a new discussion -->  <!--- Provide a general summary of the issue in the Title above -->   ### Current Behavior Error message when a position list is provided: `ValueError: The truth value of a Series is ambiguous. Use a.empty, a.bool(), a.item(), a.any() or a.all().`  ### Expected Behavior Calculate the position of edge list modifying the lines 845 - 848 of `nx_pylab.py`:  ``` # compute initial view minx = np.amin(np.ravel(edge_pos[:, :, 0]).any()) maxx = np.amax(np.ravel(edge_pos[:, :, 0]).any()) miny = np.amin(np.ravel(edge_pos[:, :, 1]).any()) maxy = np.amax(np.ravel(edge_pos[:, :, 1]).any()) ```   ### Steps to Reproduce ``` node_size = nx.get_node_attributes(G_CONS, 'postos') node_size = np.array(list(node_size.values()))  pos = nx.get_node_attributes(G_CONS, 'position')  nx.draw(G_CONS, pos = pos, with_labels=False, node_size=node_size ) ```  ### Environment <!--- Please provide details about your local environment --> Python version: 3.7.13 (default, Apr 24 2022, 01:04:09)  [GCC 7.5.0] NetworkX version: 2.6.3   ### Additional context <!--- Add any other context about the problem here, screenshots, etc. --> 

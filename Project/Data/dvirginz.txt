issue
edge weight for self-loop overlap vertex label#TITLE_END#I am trying to both present the node label and the edge label at the same time, unfortunately, as I have self-loops, the labels run on top of each other. Is there a way to define the offset from the edge itself? code: ```     pos = nx.spring_layout(netx)     edges_weights_for_vis = {(u, v): "{0:.3f}".format(d['edge_attr']) for u, v, d in netx.edges(data=True)}     nx.draw_networkx(netx, pos=pos, **options, )     nx.draw_networkx_nodes(netx, pos)     nx.draw_networkx_edge_labels(netx, edge_labels=edges_weights_for_vis, font_size=10, pos=pos, label_pos=0.2) ``` output: https://i.stack.imgur.com/NJLcA.png

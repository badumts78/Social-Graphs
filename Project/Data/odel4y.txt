issue
nx.lowest_common_ancestor incorrectly returns None for this DAG#TITLE_END#Update: See even more minimal example in https://github.com/networkx/networkx/issues/4942#issuecomment-872089937 ### Current Behavior When executing the `nx.lowest_common_ancestor` algorithm for the pair (**B**, **D**) in this graph, NetworkX incorrectly returns None.  ![ancestor-graph](https://user-images.githubusercontent.com/10086888/123952621-88ed8c80-d9a6-11eb-94e7-dafdd4badb10.png)  <!--- Tell us what happens instead of the expected behavior -->  ### Expected Behavior If I'm not wrong, the lowest common ancestor should exist and be **C**.  ### Steps to Reproduce ```python3 import networkx as nx  G = nx.DiGraph()  G.add_edge("A", "B") G.add_edge("C", "A") G.add_edge("C", "D") G.add_edge("E", "A") G.add_edge("F", "C")  # Should return "C", but returns None print(nx.lowest_common_ancestor(G, "B", "D", default=None)) ```  ### Environment <!--- Please provide details about your local environment --> Python version: 3.8.5 NetworkX version: 2.6rc2 (also tested with earlier versions)  ### Additional context For some reason, the algorithm actually returns the correct result if the adding order of the two last edges is swapped: ```python3 G.add_edge("A", "B") G.add_edge("C", "A") G.add_edge("C", "D") G.add_edge("F", "C") G.add_edge("E", "A") ```

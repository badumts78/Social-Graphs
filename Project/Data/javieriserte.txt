issue
non_randomness fail with ValueError: math domain error on simple network#TITLE_END#The calculation of non_randomness fails with a simple network.  ```python import networkx as nx g = nx.Graph() g.add_edges_from([     ('A', 'B'),     ('B', 'C'),     ('B', 'D'),     ('D', 'E')     ]) print(nx.algorithms.non_randomness(g)) ``` throws: ``` .venv\lib\site-packages\networkx\algorithms\non_randomness.py in non_randomness(G, k)      88       89     # eq. 4.5 ---> 90     nr_rd = (nr - ((n - 2 * k) * p + k)) / math.sqrt(2 * k * p * (1 - p))      91       92     return nr, nr_rd  ValueError: math domain error ``` 

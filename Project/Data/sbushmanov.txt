issue
KeyError: <class 'dict'> when saving DiGraph to graphml#TITLE_END#I'm running `networkx 2.2` and I'm trying a very simple example and getting error when saving to a `graphml` format:  ``` import networkx as nx nodes = list("abcdefhjkh") l = [np.random.choice(nodes,2) for _ in range(100)] G = nx.DiGraph(l) dups = [("a","b")] for dup in dups:     g = nx.contracted_nodes(G, *dup, self_loops=True) nx.write_graphml(g, "./g.graphml") # must be g, not H ... ... ... ~/anaconda3/lib/python3.6/site-packages/networkx/readwrite/graphml.py in add_graph_element(self, G)     652                     attributes[k] = v     653         for k, v in attributes.items(): --> 654             T = self.xml_type[self.attr_type(k, "node", v)]     655             self.get_key(make_str(k), T, "node", node_default.get(k))     656         # Edges and data  KeyError: <class 'dict'> ```  There is no error if I do not do `contracted_nodes`  Is this a bug or I am missing something?

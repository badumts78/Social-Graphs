issue
Bug: `is_minimal_d_separator` may return True even for a non d-separating set#TITLE_END#<!-- If you have a general question about NetworkX, please use the discussions tab to create a new discussion -->  <!--- Provide a general summary of the issue in the Title above --> When experimenting with `networkx` 3.0.rc1, specifically with the d-separation algorithms, I noticed a bug where a set would be non d-separating, however would still return `True` for `is_minimal_d_separator`. Interestingly, it returns `False` for `d_separated`.   ### Current Behavior ``` import networkx from networkx.algorithms.d_separation import is_minimal_d_separator, d_separated  if __name__ == '__main__':     g = networkx.DiGraph()      g.add_edges_from([('A', 'B'), ('A', 'E'), ('B', 'C'), ('B', 'D'), ('D', 'C'), ('D', 'F'), ('E', 'D'), ('E', 'F')])      assert not d_separated(g, {'C'}, {'F'}, {'D'})      # the set is not d-separated, but returns True for minimally separated.     assert is_minimal_d_separator(g, 'C', 'F', {'D'}) # < this should fail     assert is_minimal_d_separator(g, 'C', 'F', {}) # < this should fail ``` <!--- Tell us what happens instead of the expected behavior -->  ### Expected Behavior ``` import networkx from networkx.algorithms.d_separation import is_minimal_d_separator, d_separated  if __name__ == '__main__':     g = networkx.DiGraph()      g.add_edges_from([('A', 'B'), ('A', 'E'), ('B', 'C'), ('B', 'D'), ('D', 'C'), ('D', 'F'), ('E', 'D'), ('E', 'F')])      assert not d_separated(g, {'C'}, {'F'}, {'D'})     assert not  is_minimal_d_separator(g, 'C', 'F', {'D'}) # < this should pass     assert not is_minimal_d_separator(g, 'C', 'F', {}) # < this should pass ``` <!--- Tell us what should happen -->  ### Steps to Reproduce  <!--- Provide a minimal example that reproduces the bug --> ``` import networkx from networkx.algorithms.d_separation import is_minimal_d_separator, d_separated  if __name__ == '__main__':     g = networkx.DiGraph()      g.add_edges_from([('A', 'B'), ('A', 'E'), ('B', 'C'), ('B', 'D'), ('D', 'C'), ('D', 'F'), ('E', 'D'), ('E', 'F')])      assert not d_separated(g, {'C'}, {'F'}, {'D'})      # the set is not d-separated, but returns True for minimally separated.     assert is_minimal_d_separator(g, 'C', 'F', {'D'})     assert is_minimal_d_separator(g, 'C', 'F', {}) ```  ### Environment  <!--- Please provide details about your local environment -->  Python version: 3.8.12 NetworkX version: 3.0rc1  ### Additional context  <!--- Add any other context about the problem here, screenshots, etc. --> 

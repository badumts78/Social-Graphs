issue
All pairs dijkstra returning both paths and lengths#TITLE_END#Today Networkx only have `all_pairs_dijkstra_path` and `all_pairs_dijkstra_path_length`, but there is no way to get both paths and lengths at the same time (as we have in `single_source_dijkstra`)

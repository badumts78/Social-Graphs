issue
compute a random_regular_graph(N,K) but the adjacency matrix is totally wrong#TITLE_END#I will show my code to reproduce the issue. Thank you for your attention.  # 设置随机种子以获得可重复的结果 np.random.seed(42)  # 或者使用任何你喜欢的种子值  N = 10 K = 3  # 生成 k-正则图 graph = nx.random_regular_graph(K, N)  # 绘制图形 plt.figure(figsize=(6,3)) pos = nx.spring_layout(graph) nx.draw(graph, pos, node_size=200, node_color='skyblue', with_labels=True, width=0.5) plt.axis('off')  # 显示图形 plt.show()  # 计算并打印邻接矩阵以验证 adj_matrix = nx.adjacency_matrix(graph).todense() print("邻接矩阵:") print(adj_matrix) ![image](https://github.com/user-attachments/assets/146018eb-1d24-48df-9f40-060c3dd0a453) ![image](https://github.com/user-attachments/assets/32166354-4529-4f7f-84d7-33cf8d9e7b1a) 

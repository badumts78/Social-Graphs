issue
Selecting the type of marker on a node per node bases#TITLE_END#I noticed that it is not possible to select the marker (or shape) for a network on a node per node basis. Ideally I think we should be able to select the type of maker for a node as we define the color or the suze of the node.  Something like that:  ``` python for i,node in enumerate(O):     color             =  "r" if i in V2  else "grey"     node_size     = 200 if i in V2 else 50     node_shape  =  "s" if i in V2  else "o"     G.add_node(i, pos=(-pb["Longitude"][i],pb["Latitude"][i], ),color=color,node_size=node_size,node_shape=node_shape)  nx.draw(G, nx.get_node_attributes(G, 'pos'),with_labels=False,             node_color=list(nx.get_node_attributes(G, 'color').values()),             node_size=list(nx.get_node_attributes(G, 'node_size').values()),             node_shape=list(nx.get_node_attributes(G, 'node_shape').values()))  ```

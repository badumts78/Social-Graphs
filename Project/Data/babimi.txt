issue
an issue with the clique number#TITLE_END#I am trying to find the clique numbers of a list of matrices.  ``` from numpy import linalg as LA import numpy as np import networkx as nx   def split(word):     return [char for char in word]  def matrix_maker(l):     num_list = []     for element in l:         s = split(element)         ss = [int(i) for i in s]         num_list.append(ss)         x = np.array(num_list)     return x  def Clique_finder(M):     G = nx.from_numpy_matrix(M)     return graph_clique_number(G)   f = open("42size.txt", "r") g = f.read() l = g.split('-------------------------------') matrix_list = {} for i in range(0, 42):     s = l[i].split()     s.remove(s[0])     A = matrix_maker(s)     d = {i: [Clique_finder(A)]}     matrix_list.update(d)    print(matrix_list) ```  But I got the following error  `NameError: name 'graph_clique_number' is not defined `  I am wondering if somebody can help me.

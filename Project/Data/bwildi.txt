issue
Numeric assortativity coefficient does not work on weighted graphs#TITLE_END#The numeric [assortativity coefficient function](https://networkx.github.io/documentation/networkx-1.9/reference/generated/networkx.algorithms.assortativity.numeric_assortativity_coefficient.html#networkx.algorithms.assortativity.numeric_assortativity_coefficient) does not work when applied on weighted graphs -- it treats them as unweighted graphs.  Below is a code example - I generate an adjacency matrix, calculate the assortativity coefficient using NetworkX  (incorrect output) and then the correct output in Numpy. I also tried converting the weighted network to an unweighted network to see if I  would get the coefficient then, but it is still inaccurate - I'm unsure why that is the case.  ```python import networkx as nx import numpy as np import pandas as pd ```   ```python # Incorrect nx array = np.array([[5,3,0], [5,9,1],[0,3,3]]) G = nx.from_numpy_array(array) att = {0:3,1:4,2:6} nx.set_node_attributes(G, att, 'Att') print(nx.algorithms.numeric_assortativity_coefficient(G,'Att')) ```      0.4696969696969706        ```python # Correct NumPy att2 = np.array(list(att.values())) array2 = array / np.sum(array) a_x = np.sum(array2, axis=1) b_y = np.sum(array2, axis=0) stdx = np.sqrt(np.sum(att2**2 * a_x) - np.sum(att2 * a_x)**2) stdy = np.sqrt(np.sum(att2**2 * b_y) - np.sum(att2 * b_y)**2)  print(np.sum(np.outer(att2, att2) * (array2 - np.outer(a_x, b_y))) / (stdx * stdy)) ```      0.6057726688418602        ```python # Attempted nx correction df = {'source': [], 'target':[]} attrib = {} l = 0 for i in range(3):     for j in range(3):         for _ in range(array[i,j]):             df['source'].append(l)             df['target'].append(l+1)             attrib[l] = att[i]             attrib[l+1] = att[j]             l += 2 df = pd.DataFrame(df) G = nx.from_pandas_edgelist(df) nx.set_node_attributes(G, attrib, 'Att') nx.algorithms.numeric_assortativity_coefficient(G, 'Att') ```         0.5857142857142826   

issue
Node attribute "id" gets removed during serialization/deserialization#TITLE_END#### Current Behavior  When you serialize a graph that has node attributes, the "id" attribute gets silently removed.  **Before serialization:** 1 {'id': 1, 'name': 'Alice', 'age': 30} 2 {'id': 2, 'name': 'Bob', 'age': 25}  **After deserialization:** 1 {'name': 'Alice', 'age': 30} 2 {'name': 'Bob', 'age': 25}  ### Expected Behavior  Node attributes (even ones that overlap internal usage) should not be removed by serialization.  ### Steps to Reproduce  ``` import networkx as nx  # Create a graph G = nx.Graph()  # Add nodes with attributes G.add_node(1, id=1, name="Alice", age=30) G.add_node(2, id=2, name="Bob", age=25)  # Print nodes with attributes before serialization print("Before serialization:") for node, attrs in G.nodes(data=True):     print(node, attrs)  # Serialize to JSON graph_json = nx.readwrite.json_graph.node_link_data(G, edges="edges")  # Deserialize back G_deserialized = nx.readwrite.json_graph.node_link_graph(graph_json, edges="edges")  # Print nodes with attributes after deserialization print("\nAfter deserialization:") for node, attrs in G_deserialized.nodes(data=True):     print(node, attrs) ```  ### Environment Mac M1 Laptop  Python version: 3.10.13 NetworkX version: 3.4.2  
comment
Just wanted to throw in a big +1 of encouragement for this. Having a slick way of going from a Pandas dataframe to a NetworkX graph will be super useful :) 

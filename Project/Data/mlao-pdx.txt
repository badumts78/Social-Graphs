issue
Possible bug in implementation of Dijkstra's shortest path with negative weights#TITLE_END#Over the weekend, while attempting to calculate duration between nodes of a graph representing schedule dependencies using the longest path/duration between the start node and a chosen target node, I ran into this:  ```python python: sys.version_info(major=3, minor=8, micro=4, releaselevel='final', serial=0) networkx: v2.5 Graph: [(1, 2, {'duration': -1}), (1, 3, {'duration': -1}), (2, 5, {'duration': -1}), (3, 5, {'duration': -2})] Expected: [1, 3, 5] -3 Dijkstra: [1, 2, 5] -2 Bellman-Ford: [1, 3, 5] -3 ```  Here's the code: ```python import sys import networkx as nx  # v2.5  # Attempting to calculate duration between nodes # of a graph representing schedule dependencies # using the longest path/duration between the # start node and a chosen target node.  g = nx.MultiDiGraph()  g.add_edge(1, 2, 0, duration=-1) g.add_edge(2, 5, 0, duration=-1) g.add_edge(1, 3, 0, duration=-1) g.add_edge(3, 5, 0, duration=-2)  assert nx.algorithms.dag.is_directed_acyclic_graph(g)  print(f"python: {sys.version_info}") print(f"networkx: v2.5") print("Graph:", g.edges(data=True)) print("Expected: [1, 3, 5] -3") for method in ["dijkstra", "bellman-ford"]:     print(         f"{method.title()}:",         nx.algorithms.shortest_paths.generic.shortest_path(             g, source=1, target=5, weight="duration", method=method         ),         nx.algorithms.shortest_paths.generic.shortest_path_length(             g, source=1, target=5, weight="duration", method=method         ),     )  ```

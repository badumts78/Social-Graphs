issue
node labels become diamonds#TITLE_END#Hey ,pal! my code below didn't show chinses labels correctly:  fo = open('./senti_test.txt') for line in fo:     line=line.strip()     list = line.split()     for i in range(len(list)-1):          cur = list[i];follow = list[i+1]         if (dictpos.get(cur) and dictpos.get(follow)) or (dictneg.get(cur) and dictneg.get(follow)):             # Directed graph, and the weight from google.             # 不错 稳定 the relationship:the node1 supports node2;             DG.add_edge(cur.decode('utf8'),follow.decode('utf8'),weight=0.1)  nx.draw(DG) plt.show()  the file senti_test.txt is a chinese text in utf-8. Any guidance would be greatly appreciated. 

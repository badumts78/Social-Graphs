issue
from_pandas_edgelist() adds some edges backwards#TITLE_END#<!-- If you have a general question about NetworkX, please use the discussions tab to create a new discussion -->  ### Current Behavior  Some, but not all, of the edges are reversed in direction from the dataframe.  ### Expected Behavior  If I have two columns in the dataframe, "source" and "target", the resulting graph should be directed from source to target.  ### Steps to Reproduce  ``` import pandas as pd import networkx as nx import matplotlib.pyplot as plt  data = {     'source': ['A', 'B', 'C'],     'target': ['B', 'C', 'A'],     'weight': [0.5, 0.8, 0.2],     'color': ['red', 'blue', 'green'] }  df = pd.DataFrame(data)  # Create a NetworkX graph from the DataFrame G = nx.from_pandas_edgelist(df, 'source', 'target', edge_attr=True)  # Plot the NetworkX graph with metadata pos = nx.spring_layout(G) nx.draw(G, pos, with_labels=True,         arrows=True, arrowstyle='-|>',arrowsize=10,         edge_color=df['color'], width=df['weight'])  # Add edge labels edge_labels = {(row['source'], row['target']): f"Weight: {row['weight']}" for _, row in df.iterrows()} nx.draw_networkx_edge_labels(G, pos, edge_labels=edge_labels, font_color='gray', font_size=8)  # Show the plot plt.show()  # Print edge list: will show ('A', 'C', {'weight': 0.2, 'color': 'green'}) rather than ('C', 'A') as expected G.edges(data=True) ```  ### Environment  Python version: 3.11.7 NetworkX version: 3.2.1 from pip

issue
waxman_model's domain problem#TITLE_END#I think `waxman_model` doesn't process the domain correctly.  networkx/geometric.py (version 1.9.1) ---  Original version:  ``` python def waxman_graph(n, alpha=0.4, beta=0.1, L=None, domain=(0,0,1,1)):   ...     for n in G:         G.node[n]['pos']=((xmin + (xmax-xmin))*random.random(),                           (ymin + (ymax-ymin))*random.random()) ```  Fixed version:  ``` python def waxman_graph(n, alpha=0.4, beta=0.1, L=None, domain=(0,0,1,1)):   ...     for n in G:         G.node[n]['pos']=(xmin + ((xmax-xmin)*random.random()),                           ymin + ((ymax-ymin)*random.random())) ``` 
